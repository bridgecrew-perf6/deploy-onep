name: 'deploy-onep'
description: 'Register deploy with One Platform!'
author: 'Douglas Silva'
runs:
  using: "composite"
  steps:
    - run: |
        chmod +x register-deploy.sh
        sh register-deploy.sh
      shell: bash